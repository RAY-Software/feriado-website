apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ohmexico-preprod-ingress
  namespace: ohmexico
  annotations:
    kubernetes.io/ingress.class: "gce"
    networking.gke.io/v1beta1.FrontendConfig: "http-to-https-redirect"
    networking.gke.io/managed-certificates: "ohmexico-preprod-managed-cert" # Define the ManagedCertificate name
spec:
  rules:
  # Regla para el host de preproducción de Oh México
  - host: ohmexico.preprod.rayapp.io
    http:
      paths:
      # Ruta por defecto para todo el tráfico en este host (frontend)
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ohmexico-service
            port:
              number: 80 # Asegúrate de que sea el puerto correcto de tu servicio frontend para el Ingress
---
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  annotations:
  name: ohmexico-preprod-managed-cert
  namespace: ohmexico
spec:
  domains:
  - ohmexico.preprod.rayapp.io
---
apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: http-to-https-redirect
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
